{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "copy:cesium": "node scripts/copy-cesium.mjs",
    "predev": "npm run copy:cesium",
    "dev": "vite",
    "prebuild": "npm run copy:cesium",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "copy:backend": "node scripts/copy-to-backend.mjs",
    "postbuild": "npm run copy:backend"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.22",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "cesium": "^1.145.0",
    "leaflet": "^1.9.4",
    "maplibre-gl": "^6.7.0"
  }
}
